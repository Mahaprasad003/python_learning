t = (1, 2, 4, 5)
print(t[0])

# Tuples are immutable; they cannot be updated

# t1 = () # Empty Tuple
# t2 = (2,) # tuple with a single element2
# t3 = (2) # Wrong way to declare a tuple with a single element
